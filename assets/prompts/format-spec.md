# Prompt bundle format specification

Placeholder. The serialized format produced by the engine (bundle structure, section
ordering, escaping rules, versioning) will be specified here, starting in M1.
